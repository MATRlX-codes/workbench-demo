# Public demo build (Vercel / `next build`).
#
# Runs the whole app as a client-side mock with NO authentication and NO
# database, so it deploys as a shareable link with zero secrets configured.
# `next dev` is unaffected and keeps real Clerk auth for local development.
NEXT_PUBLIC_DEMO_MODE=true
